#!/usr/bin/env bash
# Toggle the monitor brightness

if [[ $(systemctl is-active sshd >/dev/null 2>&1) ]]; then
    sudo systemctl stop wg-quicker-ch.service
    notify-send "Stopped VPN Service"
else
    sudo systemctl start wg-quicker-ch.service
    notify-send "Started VPN Service"
fi
